# 🪓 Технічне завдання (ТЗ)
## Web-застосунок для приміщення для розваг «Сокирня»

---

## 1. Загальний опис проєкту

Web-застосунок призначений для:
- презентації розважального простору «Сокирня» (landing page);
- онлайн-бронювання ліній для кидання сокир;
- адміністративного керування бронюваннями.

Проєкт реалізується у форматі **MVP** з можливістю подальшого масштабування.

---

## 2. Ролі користувачів

### 2.1 Користувач (Client)
- перегляд інформації про заклад;
- створення бронювання;
- перегляд доступних часових слотів.

### 2.2 Адміністратор (Admin)
- перегляд усіх бронювань;
- фільтрація бронювань за датою;
- зміна статусу бронювання;
- видалення бронювання.

---

## 3. Години роботи та часові правила

- Години роботи (тестові): **10:00 – 19:00**
- Мінімальний крок часу: **30 хвилин**
- Останній можливий старт бронювання: **18:30**

Години роботи зберігаються у конфігурації бекенду (не хардкодяться на фронті).

---

## 4. Лінії для кидання

- Кількість ліній: **3**
- Кожна лінія бронюється окремо
- Лінія **обирається користувачем** при бронюванні

---

## 5. Бронювання

### 5.1 Дані бронювання

Користувач вводить:
- імʼя;
- номер телефону;
- дату;
- час початку;
- тривалість (кратна 30 хв);
- номер лінії (1–3).

### 5.2 Модель часу

У БД зберігається:
- `startTime` — дата та час початку;
- `durationMinutes` — тривалість у хвилинах.

`endTime` обчислюється динамічно:
```
endTime = startTime + durationMinutes
```

---

## 6. Перевірка доступності слотів

Слот вважається зайнятим, якщо існує бронювання з перетином часу:
```
A.start < B.end && A.end > B.start
```

Недоступними вважаються бронювання зі статусами:
- `pending`
- `confirmed`

Перевірка доступності виконується **на бекенді перед створенням бронювання**.

---

## 7. Статуси бронювання

- `pending` — створене, очікує дзвінка адміністратора;
- `confirmed` — підтверджене адміністратором;
- `canceled` — скасоване адміністратором.

Нові бронювання створюються **зі статусом `pending`**.

---

## 8. Адміністративна панель

### 8.1 Функціонал

- перегляд списку бронювань;
- фільтр за датою;
- перегляд деталей бронювання;
- зміна статусу (`pending / confirmed / canceled`);
- видалення бронювання.

### 8.2 Авторизація

- доступ тільки для адміністратора;
- авторизація через логін + пароль;
- використання JWT.

---

## 9. Landing Page

Landing містить:
- опис розваги;
- інформацію про правила;
- години роботи;
- кнопку переходу до бронювання.

SEO-оптимізований, з SSR.

---

## 10. Технологічний стек

### Frontend
- Next.js
- TypeScript
- UI-бібліотека (на вибір)

### Backend
- NestJS
- REST API
- DTO + ValidationPipe

### Database
- PostgreSQL
- ORM: Prisma

---

## 11. Основні API (попередньо)

### Public
- `GET /working-hours`
- `GET /available-slots?date=&line=&duration=`
- `POST /bookings`

### Admin
- `POST /auth/login`
- `GET /admin/bookings`
- `PATCH /admin/bookings/:id/status`
- `DELETE /admin/bookings/:id`

---

## 12. Вимоги до якості

- валідація даних на бекенді;
- захист від подвійного бронювання;
- читабельний та масштабований код;
- готовність до розширення (оплати, повідомлення, статистика).

---

## 13. Майбутні покращення (поза MVP)

- оплата онлайн;
- email / SMS повідомлення;
- календар для адміна;
- аналітика завантаженості ліній.

---

**Документ є базою для реалізації MVP та подальшого розвитку проєкту «Сокирня».**

